// ---------------------------------------------------------------------------
// Semantic event types — re-exported for convenience
// ---------------------------------------------------------------------------

export type {
  ParserEvent,
  ValueCommittedEvent,
  ContainerClosedEvent,
  RepairAppliedEvent,
  DiagnosticEvent,
  DocumentCompleteEvent,
  StreamFinishedEvent,
} from "../types.js";
